default: true

MD003: # heading-style
  style: atx
MD004: # ul-style
  style: dash
MD013: false # line-length - does not support editorconfig
MD024: # no-duplicate-heading
  siblings_only: true
MD026: # no-trailing-punctuation
  punctuation: .,:;
MD029: # ol-prefix
  style: ordered
  # Note: This rule will interfere with prettier when there are over ten ordered list items.
  # Wrap your list items with this to circumvent:
  # <!-- markdownlint-disable ol-prefix -->
  # <!-- markdownlint-enable ol-prefix -->
MD030: # list-marker-space - conflict with prettier
  ol_multi: 2
  ol_single: 2
  # Note: This rule will interfere with prettier when there are over ten ordered list items.
  # Wrap your list items with this to circumvent:
  # <!-- markdownlint-disable list-marker-space -->
  # <!-- markdownlint-enable list-marker-space -->
MD033: # no-inline-html
  allowed_elements:
    - a
    - br
    - dd
    - details
    - div
    - dl
    - dt
    - embed
    - figure
    - figcaption
    - img
    - li
    - nav
    - section
    - strong
    - summary
    - sup
    - ul
MD034: false # no-bare-urls
MD046: false # code-block-style - does not support admonitions
MD048: false # code-fence-style - does not support admonitions
MD049: # emphasis-style
  style: underscore
MD050: # strong-style
  style: asterisk
